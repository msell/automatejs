var sut = require('../../triggers/scheduledTask.js'),
    chai = require('chai'),
    should = require('chai').should(),
    chaiAsPromised = require('chai-as-promised'),
    sinon = require('sinon'),
    RSVP = require('rsvp');

describe('scheduled tasks', function(){
    describe('when it is time to execute a task',function(){
       it('should resolve the promise');
          //, function(done){                      
          // sut.should.be.rejectedWith().and.notify(done);
       //})
    })
})